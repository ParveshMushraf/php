version: '3'
services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./src:/var/www/html
    ports:
      - 8080:80
    depends_on:
      - mysql
  mysql:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: naveen
      MYSQL_DATABASE: db_snk
      MYSQL_USER: naveen
      MYSQL_PASSWORD: naveen
    volumes:
      - mysql_data:/var/lib/mysql
volumes:
  mysql_data: